// Filled in once after the Supabase project is set up (see /supabase/README.md).
// Only the public anon key goes here -- real protection is server-side
// (Supabase Auth + the RLS-scoped v_stok_tersedia view in /supabase/schema.sql).
window.BUKUGUDANG_CONFIG = {
  SUPABASE_URL: 'https://yektvmozyxfuzlxjycwu.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_MMCViDLRyEU-E2eaws93PA_7tei24WB'
};
